import {SafeUrlPipe} from './safe-url';
import {NgModule} from '@angular/core';

/**
 * Module containing the Safe URL pipe.
 */
@NgModule({
  imports: [
  ],
  declarations: [
    SafeUrlPipe
  ],
  exports: [
    SafeUrlPipe
  ]
})

export class SafeUrlModule { }
